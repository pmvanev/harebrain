[project]
name = "hello_world"
version = "0.0.0"
description = "Smoke test for the harebrain Python workspace."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hello_world"]
